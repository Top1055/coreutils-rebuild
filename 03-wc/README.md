## 03 — wc

Counts lines, words and bytes. Matches GNU wc byte-for-byte
including column alignment.

**Learned:** getopt and optind, printf's `*` width specifier,
signed char vs unsigned char with ctype functions, why wc stats
files before reading them. Passing pointers through functions

**Known gaps:** no multibyte character counting (-m).
